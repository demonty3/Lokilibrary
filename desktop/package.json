{
  "name": "libraryworld-desktop",
  "version": "0.0.1",
  "private": true,
  "description": "Electron wrapper for LibraryWorld (Phase 6).",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && electron .",
    "dev": "tsc && cross-env LIBRARYWORLD_RENDERER_URL=http://localhost:5183 electron ."
  },
  "dependencies": {
    "steamworks.js": "^0.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "cross-env": "^7.0.3",
    "electron": "^32.0.0",
    "typescript": "^6.0.0"
  }
}
