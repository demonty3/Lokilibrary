# Diorama neighbour — a new question, inheriting the 09 kill record

**2026-07-31.** Per project protocol, revisiting a settled question means a new
document that inherits the old bars verbatim; it never means editing the old one.
This is that document. The settled question stays settled. What follows opens a
different one.

## 1 · The inherited kill record (verbatim, sources named)

The record every reader of this document is bound by. Quotes keep their original
punctuation.

**STATE.md, ceiling-widening entry (2026-07-31):**

> Harry's verdict session: packs should be able to read as *different machines*,
> not recolours — the in-medium ceiling (style round 01–08) is PURSUE; the
> out-of-medium provocations (09 diorama / 10 manuscript) stay killed.

**The 09 card, `2026-07-30-style-directions.html`, Flags line and banner:**

> Cites the record: sprites-on-6px-cells was confirmed dead twice (2026-07-16
> fidelity test) and glyphs won the bake-off. This card is a what-if of a
> DIFFERENT engine, shown so the road not taken stays visible.

> BEYOND GLYPH · abandons pixel-art-that-reads-as-terminal — the project's
> stated medium and moat.

**The sprite fidelity test, `2026-07-16-sprite-fidelity-test.md`, Verdict:**

> 1. **C (16×32 displayed) is confirmed dead** — with *real* art this time, not
>    placeholders. [...] the beings vanish under furniture; floor, marks, and
>    scatter are buried; glance legibility collapses.
> 2. **B (native 6×13) is the interesting one.** [...] But as-tested it loses to
>    baseline on the attention contract: full-accent spine colours out-shout the
>    beings (the pink `L` disappears beside pink books) [...]
> 3. **A (glyphs) still wins today.** Calmer, warmer, the beings own their
>    accents.

**CLAUDE.md, the standing rail:**

> **Pre-v1.0 MVP renders with box-drawing glyphs only** — pixel-art comes later.

Scope of the kills, for precision: (1) sprites displayed larger than one glyph
cell, twice; (2) uncurated sprite loading; (3) sprites versus glyphs *inside the
same glyph scene*, on the attention contract; (4) a raster engine *replacing*
the desk's renderer (the 09 card). No kill touched the Phase 3 template-build
pipeline, and none examined what this document opens.

## 2 · The new question

The killed question was: should a hi-bit diorama replace the glyph desk's
medium? Killed, twice cited, stays killed.

The new question is: **can a hi-bit diorama world exist as a neighbour** — its
own terminal window snapped to the glyph desk, one palette per scene, the seam
as the boundary, beings walking between the two and re-embodied by each side's
renderer as they cross?

This shape already has doctrinal support. VISION.md:

> **Per-terminal identity.** Each window its own palette and vocabulary
> (one palette per *scene* survives — the seam is the boundary), so a join
> reads as a portal between genuinely different places.

And `docs/PRD-snapping-terminals.md`, T3 (parked, not rejected):

> Per-terminal theming hooks (one theme per terminal scene — themes may
> differ BETWEEN terminals; the join edge is the boundary)

Engine facts established 2026-07-31 (exploration, recorded here for costing):
the theme is already a plain per-window parameter; a crossing being carries
behaviour only (no glyph, no colour — the receiving window re-resolves its
visual); the seam silhouette is pure, theme-free geometry. Coexistence is
architecturally close. The diorama window itself is not: it is a second land
painter plus a real art pipeline, and every conformance gate we have measures
glyph and palette maths. That cost is why this document commits to a mockup
and nothing else.

## 3 · Kill condition — frozen 2026-07-31, before the mockup was first viewed

The known risk is dilution by fidelity gradient: the eye may treat the higher
fidelity window as the real one and the glyph window as the cheap one. That is
the attention-contract failure at world scale, the same failure that killed
sprites-in-scene twice. So, frozen now:

> **If the glyph window reads as downgraded beside the diorama window, if the
> eye refuses to return to it, coexistence fails and the diorama stays dead at
> every scope.**

Pass is not the mirror image: a pass means the pair reads as a portal between
genuinely different places, and each side survives being looked at after the
other. If it passes, the question graduates to a spike *only* once the
preconditions below are met. This bar does not soften after viewing; if the
mockup misleads (authoring artefacts, wrong registration), fix the mockup and
look again — never reword the bar.

## 4 · Preconditions for any engine work (both unmet today)

No engine work follows from any outcome of the mockup until both hold:

1. **The in-medium round is resolved.** The 01–08 PURSUE thread (omission axis,
   light ground, cold run 3) reaches its verdicts, so the glyph desk is
   compared at full strength, not at its recolour-era floor.
2. **One Phase 3 sprite survives curation.** The open TODO-USER.md item ("Bake
   real PixelLab sprites"): bake candidates, Harry picks one survivor, it holds
   up on screen. A raster *world* before one raster *sprite* has survived the
   eyeball would invert the project's whole curation ladder.

## 5 · The probe

The mockup: `2026-07-31-diorama-neighbour.html` — a static joined pair, the
glyph desk and a hi-bit diorama sharing one seam and one terrain silhouette,
one being mid-crossing, drawn as each machine would draw her. Judge it at
wallpaper distance against §3. React keep / kill / mutate; the verdict is
recorded in this file, dated, below this line.

---

## Verdict — 2026-07-31, Harry's eyeball (same day the probe opened): MUTATE

Harry, verbatim: "I think it's working but the design choice of the terminal
is unbearable to look at. I think this is a mutate option."

Read against §3: the kill condition did **not** fire. Nothing in the reaction
says the glyph window reads as downgraded, or that the eye refuses to return
to it — "I think it's working" is a read of coexistence. What fails is the
mockup's own presentation: a design choice of "the terminal" reads as
unbearable. Which element he means — the diorama window's art direction, the
window chrome framing the pair, or the glyph side's styling — was not pinned
in-session and must be pinned before any re-mock.

Per §3's own rule this is the fix-the-mockup-and-look-again path: the bar does
not soften, a re-mock inherits it verbatim, and §4's two preconditions for
engine work remain unmet and untouched. Until a re-mock is judged, the probe's
standing is: **coexistence unrefuted, unconfirmed.**

### The element, pinned — 2026-08-01

Harry, verbatim: "i think it's the glyph style all of those ae's are
unreadable. Might be fine when there are more terminals already on the go but
If i was to open this up on the first time and i couldn't understand the world
I would be confused."

Identified on the rendered page: the "ae's" are the glyph desk's dense
shade-dither ground band — Cozette's medium-shade glyphs, whose dot-columns at
reading size read as rows of squashed letterforms rather than as material.
There are no letter glyphs in that band; the *read* is letter-noise anyway.

Scope correction that follows: the mockup's glyph side reproduces the shipped
desk faithfully ("every grade real"), and the real desk shots show the same
dotted crust patches. So the mutate does not route to mockup rework — it is a
**first-contact legibility finding about the shipped desk's crust register**,
surfaced by adjacency to a machine with no such ambiguity. Harry's own caveat
bounds it: context may teach the reading once several terminals are alive; the
risk is first contact (the clone-and-run stranger). The fix therefore lives on
the depth track (land legibility/polish); a re-mock only makes sense *after*
the desk's ground band improves, re-shot from the improved desk, inheriting
§3's bar verbatim.
