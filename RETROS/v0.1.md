# v0.1 retrospective

PLAN.md (line 165) mandates this retro at the end of Phase 1. The question
SPEC §9 raises explicitly: **did the painted-3D + LLM combination deliver
the magic the 2D prototype couldn't?** Fill in after walking the world.

## What v0.1 ships

Phase 1.1 through 1.10 of PLAN.md, code-only. Outstanding hand-work below.

| Sub-task | Status |
|---|---|
| 1.1 Scaffold check | done (audit, no code) |
| 1.2 Empty scene + ground | done (pre-existing) |
| 1.3 Rapier physics | done (pre-existing) |
| 1.4 First archetype (lighthouse) | done as Lighthouse component |
| 1.5 Interact prompt + E handler | done (`src/scene/archetypes/useInteract.ts`) |
| 1.6 Cloudflare Worker | done (`worker/`, both Anthropic + Ollama branches) |
| 1.7 Meshy hero asset pass | **OUTSTANDING** — five hero archetypes ship as procedural placeholders. Meshy curation pass is hand-work, not delegated. |
| 1.8 Manifest → scene assembly | done (`src/ai/`, `src/api/`, `src/scene/CastedWorld.tsx`) |
| 1.9 Launch ritual + steam://run | done (lighthouse + four others, all wired) |
| 1.10 Return ritual | done (focus-event proxy, per CLAUDE.md acceptable through v0.5) |

## Outstanding before this retro is meaningful

1. **Meshy hero pass (Phase 1.7).** Five archetypes (lighthouse, fish market,
   detective's office, harbour master's hut, fishing boat) are procedural
   primitives. They're functional but not the visual bar — Meshy curation
   is the gating quality lever for whether v0.1 actually pops.
2. **Walk the loop.** `npm run dev` + `npm run worker` (separate terminal).
   Walk up to each archetype, press E, confirm Steam opens the right game.

## Decision

- [ ] Continue to Phase 2 (Steam OpenID + HLTB)
- [ ] Rework the approach (write a note in IDEAS.md for what changes)
- [ ] Pause; the painted-3D bar is too expensive to maintain

## If continuing to Phase 2

PLAN.md Phase 2 needs:
- Steam Web API key (`STEAM_WEB_API_KEY` in `worker/.dev.vars`)
- A Steam OpenID redirect URL whitelisted in the worker's dev origin list
- HowLongToBeat library (community-scrape; pin a version since the upstream
  endpoint shape drifts)
