# v0.1 retrospective — STUB, NOT YET FILLED IN

PLAN.md (line 165) mandates this retro at the end of Phase 1. The question
SPEC §9 raises explicitly: **did the painted-3D + LLM combination deliver
the magic the 2D prototype couldn't?** Answer honestly. If it's "almost" —
that's the moment to rework the approach, not push into the v0.2 → v1.0 arc.

This stub is what's been built; the rest is for you to fill in after
walking the world yourself.

## What v0.1 ships

Phase 1.1 through 1.10 of PLAN.md, code-only. Outstanding hand-work below.

| Sub-task | Status |
|---|---|
| 1.1 Scaffold check | done (audit, no code) |
| 1.2 Empty scene + ground | done (pre-existing) |
| 1.3 Rapier physics | done (pre-existing) |
| 1.4 First archetype (lighthouse) | done as Lighthouse component |
| 1.5 Interact prompt + E handler | done (`src/scene/archetypes/useInteract.ts`) |
| 1.6 Cloudflare Worker | done (`worker/`, both Anthropic + Ollama branches) |
| 1.7 Meshy hero asset pass | **OUTSTANDING** — five hero archetypes ship as procedural placeholders. Meshy curation pass is hand-work, not delegated. |
| 1.8 Manifest → scene assembly | done (`src/ai/`, `src/api/`, `src/scene/CastedWorld.tsx`) |
| 1.9 Launch ritual + steam://run | done (lighthouse + four others, all wired) |
| 1.10 Return ritual | done (focus-event proxy, per CLAUDE.md acceptable through v0.5) |

## Outstanding before this retro is meaningful

1. **API key.** Drop `ANTHROPIC_API_KEY=sk-ant-…` into `worker/.dev.vars`
   (or `LLM_PROVIDER=local` if you'd rather burn local Ollama for the eval).
   Until then the scene runs from the stub manifest only.
2. **Meshy hero pass (Phase 1.7).** Five archetypes (lighthouse, fish market,
   detective's office, harbour master's hut, fishing boat) are procedural
   primitives. They're functional but not the visual bar — Meshy curation
   is the gating quality lever for whether v0.1 actually pops.
3. **Walk the loop.** `npm run dev` + `npm run worker` (separate terminal).
   Walk up to each archetype, press E, confirm Steam opens the right game.

## Questions to answer

Write your own answers under each — be honest, not generous.

### Did the painted-3D + LLM combination deliver the magic?

> _your answer_

### Specifically: do the role-text lines feel personal, or generic?

(They're generated by Claude per game from a behavioral profile — at v0.1 that
profile is empty, so the role text is essentially "what would a human cast this
genre as?" which is the lowest bar. If even that feels generic, the Phase 2
behavioral profile work needs to land before judging.)

> _your answer_

### Does the recognition face beat work?

(The "oh I own that" moment when you see your game's Steam header on the
archetype's recognition panel. If this lands, every other beat is built on
top of it. If it doesn't, the recognition-face rule needs rethinking.)

> _your answer_

### Do the launch rituals feel diegetic or like a loading screen?

(They're 1.8s pre-launch, 0.4s hold per CLAUDE.md spec. A loading screen is
fine — that's their job — but they need to feel like part of the world,
not a transition out of it.)

> _your answer_

### Does the return ritual deliver "you came home"?

(Currently a brief warm overlay tween triggered by window focus — the
focus-as-return lie SPEC §2.2 calls out. Will it carry until v0.6's
Steamworks SDK integration?)

> _your answer_

## Decision

- [ ] Continue to Phase 2 (Steam OpenID + HLTB)
- [ ] Rework the approach (write a note in IDEAS.md for what changes)
- [ ] Pause; the painted-3D bar is too expensive to maintain

## If continuing to Phase 2

PLAN.md Phase 2 needs:
- Steam Web API key (`STEAM_WEB_API_KEY` in `worker/.dev.vars`)
- A Steam OpenID redirect URL whitelisted in the worker's dev origin list
- HowLongToBeat library (community-scrape; pin a version since the upstream
  endpoint shape drifts)
