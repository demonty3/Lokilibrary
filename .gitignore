node_modules
dist
tsconfig.tsbuildinfo
.DS_Store
*.log
.env
.env.local
.env.*
.vite

# Cloudflare Worker secrets — never commit
worker/.dev.vars
*.dev.vars
.wrangler/

# Phase 3C bake-script staging directory. The bake script writes N
# PixelLab candidates here per (slot, theme); the human curator picks
# the survivor and copies it to public/sprites/{theme}/{slot}.png.
staging/

# Editor / OS
.idea/
.vscode/
Thumbs.db

# local agent tooling (graphify knowledge-graph output, not a deliverable)
graphify-out/
